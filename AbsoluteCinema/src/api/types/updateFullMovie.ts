export type MovieUpdateRequest = {
        name: string,
        description: string, 
        rate: number, 
        ageLimit: number, 
        durationSeconds: number, 
        country: string, 
        studio: string,
        language: string,
}